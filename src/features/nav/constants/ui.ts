/** Layout / motion tokens owned by the nav feature only. */
export const NAV_Z_INDEX = 50;

export const NAV_MOTION = {
  linkStaggerMs: 40,
  overlayDuration: 0.25,
  linkDuration: 0.28
} as const;
